interface VerifyCodeButtonProp {
  isActive: boolean;
}

export default function VerifyCodeButton(props: VerifyCodeButtonProp) {
  const { isActive } = props;

  return <div>verifyCodeButton</div>;
}
