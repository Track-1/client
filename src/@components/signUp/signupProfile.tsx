import React from 'react'
import { SetStepPropsType } from '../../type/signUpStepTypes';

export default function SignupProfile(props:SetStepPropsType) {
    const {setStep}=props;

  return (
    <div>signupProfile</div>
  )
}
