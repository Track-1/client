import { useRecoilState } from 'recoil';
import { PlayingAudio } from '../recoil/common/audio';

function usePlay() {
  const [playingAudio, setPlayingAudio] = useRecoilState(PlayingAudio);


  

  return


}
